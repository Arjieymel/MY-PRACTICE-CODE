function printName(f_name, l_name) {
    fullName = f_name + " " + l_name; 
    completeName = "My Full name is " + fullName; 
    return completeName; 
}

var name = printName("Rj-Mel", "Dogello");
console.log(name); 
